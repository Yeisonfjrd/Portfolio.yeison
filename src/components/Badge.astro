---
const { class: className } = Astro.props;
---

<span class={`inline-flex items-center bg-gradient-to-r from-violet-900/50 to-indigo-900/50 text-violet-300 text-xs font-medium px-3 py-1.5 rounded-full border border-violet-700/50 ${className}`}>
  <slot />
</span>